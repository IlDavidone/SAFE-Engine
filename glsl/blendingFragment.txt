#version 330 core

out vec4 FragColor;

in VS_OUT {
    vec4 vertexColor;
    vec2 texCoord;
    vec3 FragPos;  
    vec3 Normal;
} fs_in;

uniform sampler2D texture1;

void main()
{
    FragColor = texture(texture1, fs_in.texCoord);
}